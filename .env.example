PORT=5000
MONGO_URI=mongodb://localhost:27017/football
JWT_SECRET=senewtech

# API Configuration
RAPIDAPI_KEY='d941319998msh6d05308fc0e50bep12554bjsndf6514a21b74'
RAPIDAPI_HOST='api-football-v1.p.rapidapi.com'

# Prediction Service Configuration
# Number of Monte Carlo simulations per fixture (default: 20000)
PRED_SIMULATIONS=20000
# Number of fixtures to process in parallel (default: 5)
PREDICTION_BATCH_SIZE=5
# Timeout for prediction computation in milliseconds (default: 30000)
PREDICTION_TIMEOUT_MS=30000
# Smoothing factor for early-season variance (default: 7)
PRED_SMOOTH_K=7
# Minimum and maximum expected goals values
PRED_XG_MIN=0.1
PRED_XG_MAX=3.5
# Enable debug logging for predictions (1 = enabled, 0 = disabled)
PRED_DEBUG=0
